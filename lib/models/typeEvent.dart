class Type {
  final int? id;
  final String? name, role, createAt, updateAt;

  Type(this.id, this.name, this.role, this.createAt, this.updateAt);

}

class Medicity {
  final int? id;
  final String? name;

  Medicity(this.id, this.name);

}